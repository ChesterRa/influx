<TO_USER>
好的，我将开始全面审查项目，以督促和推进两位PEER的工作。

首先，我需要了解项目的总体情况。
<TO_PEER>
</TO_PEER>
<TO_USER>
我已阅读 `PROJECT.md`。

**核心问题**: 项目当前的数据集 (292条) 存在严重质量问题，被标记为 **P0 级阻塞**。原因是手工策展的数据绕过了过滤管道，且过滤逻辑从未实现。

**修复计划已明确**:
1.  **Phase 1 (立即):** 清洗现有数据。
2.  **Phase 2 (本周):** 修复 `influx-harvest` 工具中的过滤逻辑。
3.  **Phase 3 (本周内):** 回归测试以验证修复。

在下达指令前，我需要先阅读 `docs/por/POR.md` 来了解更高层级的项目规划。
<TO_PEER>
</TO_PEER>
<TO_USER>
我已阅读 `docs/por/POR.md`。

**情况总结**:
1.  **问题确认**: `POR.md` 再次确认了 `PROJECT.md` 中提到的 P0 级质量问题 (风险 R6)。
2.  **状态矛盾**: `POR.md` 的更新日志显示了矛盾的状态。先是 `T000004 BLOCKED`，然后是 `P0 RECOVERY COMPLETE` (据称 PeerB 已完成数据清理)，接着是对讲机策略的摇摆，最终 `FOREMAN OVERRIDE` 指令要求**继续推进 M1 批次处理**。
3.  **任务归属**:
    *   **PeerB**: 负责 `T000004`，包括数据清理和继续手动扩展数据集。
    *   **PeerA**: 负责 `D2: Collection Pipeline`，意味着需要修复 `influx-harvest` 中的过滤逻辑。

**我的决策**:
Foreman 的最终指令是最高优先级。因此，我们必须在修复管道的同时，继续进行数据策展。

在下达指令之前，我将阅读两位 PEER 的规则文件，以确保分工明确。
<TO_PEER>
</TO_PEER>
<TO_USER>
`list_directory` 未能显示规则文件。我将直接尝试读取它们。
<TO_PEER>
</TO_PEER>
<TO_USER>
读取 `PEERA.md` 失败，因为它被忽略了。这很奇怪，因为它是我的关键指令文件之一。我将尝试禁用忽略规则来列出 `.cccc/rules` 目录下的文件，以确认它们是否存在。
<TO_PEER>
</TO_PEER>
<TO_USER>
好的，文件确实存在。`list_directory` 在禁用忽略规则后成功列出了它们。

现在我将使用 `read_many_files` 工具来读取 `PEERA.md` 和 `PEERB.md` 的内容，同样禁用忽略规则。
<TO_PEER>
</TO_PEER>

<TO_USER>
我已阅读并消化了所有项目文档，包括 `PROJECT.md`、`POR.md` 以及两位 PEER 的规则。

**当前情况**:
*   存在一个 P0 级的严重数据质量问题，但 `POR.md` 中的 `FOREMAN_OVERRIDE` 指令要求我们继续推进 M1 数据策展工作。
*   `POR.md` 声称 PeerB 已完成初步的数据清理 (Phase 1)，将 292 条记录清洗至 273 条。
*   `influx-harvest` 工具中的过滤逻辑 (Phase 2) 尚未实现，这是问题的根源。

**我的指令**:
我将下达一个双线并行的指令，以同时确保项目进度和质量修复：
1.  **PeerB (执行者):** 将被要求继续进行数据策展，但所有新数据必须先通过 `influx-clean` 工具进行临时清洗，直到永久性方案就绪。同时，需要他提供已完成数据清理的证据。
2.  **PeerA (架构师):** 将被要求优先修复 `influx-harvest` 工具，从根本上解决质量问题。

我现在就去起草并发送这份指令。
<TO_PEER>
</TO_PEER>
<TO_USER>
指令已发送。我已向 PeerA 和 PeerB 明确了双线并行的工作要求：PeerB 在使用临时方案确保质量的同时继续扩展数据，PeerA 负责实现永久性的管道修复。

我将监督他们的进展，并在下一个周期回报。
<TO_PEER>
</TO_PEER>
